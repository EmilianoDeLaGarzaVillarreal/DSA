class Stack:
    TOP = -1
    values = []

    def __init__(self, values = []):
        TOP += len(values)
        self.values = values

    def push(self, val):
        values.append(val)

    def pop(self):
        return 0